export interface Series{
    id: number,
    seriesName: string,
    status: string,
    firstAired: string,
    network: string,
    overview: string,
    filename: string
}

export interface Cover{
    cover: string;
}